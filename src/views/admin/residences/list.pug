extends ../_layout.pug

block container
  if data.length === 0
    div.alert.alert-info No existen residencias cargadas en el sistema.
  else 
    table.table.table-striped
      thead.thead-dark
        tr
          th Id
          th Titulo
          th Descripcion
          th Acciones

      tbody
        for item in data
          tr
            td= item.id
            td= item.title
            td= item.description
            td
              a(
                href=`/admin/residences/${item.id}`
                class="btn btn-link"
              ) Ver detalle
              a(
                href=`/admin/residences/${item.id}/remove`
                class="btn btn-link"
              ) Eliminar
              